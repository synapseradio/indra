// Emits the TypeScript client into ../baml_client (runtime/baml_client),
// which is gitignored and regenerated by `bun run generate`.
generator typescript {
  output_type "typescript"
  output_dir "../"
  version "0.222.0"
}
