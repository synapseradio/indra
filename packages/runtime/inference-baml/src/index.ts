/**
 * @indra/runtime-inference-baml — the live BAML adapter behind the inference
 * port.
 *
 * This package is a placeholder in this change. The live layer, `baml_src/`, and
 * the generated client move here in the `extract-inference-adapter` change;
 * until then the BAML seam stays inside `@indra/runtime-core`.
 */

export {};
