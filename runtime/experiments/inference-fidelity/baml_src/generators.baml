// Emits the TypeScript client into ../baml_client, which is gitignored and
// regenerated by the experiment's run instructions (see ../README.md).
generator typescript {
  output_type "typescript"
  output_dir "../"
  version "0.222.0"
}
