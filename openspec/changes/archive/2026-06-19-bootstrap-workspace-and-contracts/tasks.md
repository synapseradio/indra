## 1. Scaffold the workspace structure

- [x] 1.1 Write the `scripts/scaffold/` Bun CLI (zero-dep, manifest-driven, idempotent, structure and configs only — no source moves): a subcommand per part (`package-json`, `tsconfig --kind`, `rslib`, `dirs`, `root-tsconfig`) composed by `package`/`all`, fed by the five-entry `PACKAGES` manifest (`relpath|name|deps`)
- [x] 1.2 Run the scaffold to create `packages/runtime/{contracts,core,choreography,inference-baml,host}`, each with `src/`, `test/`, the tsconfig trio + `tsconfig.build.json`, `rslib.config.ts`, and `package.json` (ESM, `catalog:` deps, the `exports` map, per-package scripts)
- [x] 1.3 Write the root `tsconfig.base.json`, root solution `tsconfig.json` (references every package `tsconfig.json`), and `tsconfig.build.json` (references every package `tsconfig.build.json`)

## 2. Author the root tooling configs

- [x] 2.1 Author the root `package.json` with Bun workspaces (`packages/*`, `packages/runtime/*`) and the catalogs (`effect`, `xstate`, `@boundaryml/baml`; tooling catalog for typescript/rslib/turbo/biome/markdownlint-cli2/vitest), all `~`-pinned
- [x] 2.2 Author `turbo.json` (Turbo 2.x `tasks` key) with the build/check/lint task graph and caching
- [x] 2.3 Author `biome.jsonc` (`json.parser.allowComments: true`) and `.markdownlint.jsonc` (rules) plus `.markdownlint-cli2.jsonc` (globs/ignores); wire `lint:docs` to `markdownlint-cli2`, with `lint:fix`/`lint:fix:unsafe`/`lint:docs:fix` autofix scripts
- [x] 2.4 Stand up the `@indra/configs` package exposing the shared `vitest` base at `@indra/configs/vitest`; per-package `vitest.config.ts` is generated by the scaffold and extends it via `mergeConfig` (detailed in section 5)
- [x] 2.5 Add the `[intent]:[scope]?:[capability]?` scripts and document them in `docs/scripts.md`

## 3. Relocate source into packages (no behavior change)

- [x] 3.1 Move `ast/types.ts`, `effect/errors.ts`, and the provisional core↔choreography contract (`TurnOutcome`, events, leaf signatures) into `@indra/runtime-contracts`; mark the shared contract provisional in-package
- [x] 3.2 Move `effect/` (world store, turn evaluator, validation, assembly) into `@indra/runtime-core`; place `skeleton.ts` provisionally in `core/src/programs/`. `baml/` stays inside core for now
- [x] 3.3 Move `xstate/` (conductor, actor machine, leaves, events) into `@indra/runtime-choreography`
- [x] 3.4 Move the entrypoint into `@indra/runtime-host`
- [x] 3.5 Repoint all cross-package imports to `@indra/runtime-*` specifiers and wire path `~/*` → `./src/*` per package

## 4. Verify the boundaries hold

- [x] 4.1 `tsc -b tsconfig.json` is green from the repo root
- [x] 4.2 `@indra/runtime-contracts` resolves no workspace package in its dependency graph
- [x] 4.3 `@indra/runtime-core` imports nothing from `@indra/runtime-choreography`
- [x] 4.4 The offline test suite is green
- [x] 4.5 `turbo run check:types` is a cache hit on the second run
- [x] 4.6 Add the contracts-extraction and core/choreography-split entries to the monorepo-bootstrap initiative ledger

## 5. Shared test configs, scaffold README, and the turbo test gate

- [x] 5.1 Add a `readme` generator to `scripts/scaffold/parts.ts` that produces `<relpath>/README.md` from the manifest entry (H1 package name, conventional section headings, no subsystem assumption); wire it into the `packageScaffold` composition and add a `readme` subcommand to `scripts/scaffold/index.ts`
- [x] 5.2 Create the `@indra/configs` package exposing `baseConfig` at the `@indra/configs/vitest` exports subpath: the `vite-tsconfig-paths` plugin (`configNames` for `tsconfig.src.json`/`tsconfig.test.json`) for `~/*`, a `resolve.alias` map built from `PACKAGES` for `@indra/runtime-*` and the `@indra/runtime-core/inference-live` subpath, and shared `test` settings (node env, include/exclude, `passWithNoTests`)
- [x] 5.3 Rewrite the `vitestConfig` generator to emit a thin `vitest.config.ts` that `mergeConfig`s `baseConfig` from `@indra/configs/vitest` with this package's `test.name` and `test.root`; add `@indra/configs` as a workspace devDep in the generated `package.json`
- [x] 5.4 Regenerate all packages with the scaffold (`all`) and confirm the working tree matches the generators
- [x] 5.5 Update `turbo.json` so the `test` task declares `dependsOn` on `check:types` and `^check:types` (alongside the generate edges), register `packages/configs/vitest/**` and the scaffold manifest as global dependencies, and confirm the root `test` script runs through `turbo run test`

## 6. Verify the test configuration

- [x] 6.1 State the expectation first: the offline suite resolves cross-package and transitive `@indra/runtime-*` imports and `~/*` to source with no build; run it via the package script and confirm green
- [x] 6.2 `turbo run test` is a cache hit for an unchanged package on a second run, and a change to one package does not rerun another's tests
- [x] 6.3 Type-checking runs before tests under turbo (the `test` task waits on `check:types`)
- [x] 6.4 `tsc -b` from the repo root is green and still resolves cross-package types through project references
- [x] 6.5 Every package directory contains a generated `README.md`
